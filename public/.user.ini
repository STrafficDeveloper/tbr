; Read by PHP-FPM/CGI (standard on cPanel). Lets the admin panel accept
; photos straight off a phone; the app itself still enforces 8MB for admins
; and 2MB for member avatars.
upload_max_filesize = 10M
post_max_size = 12M
